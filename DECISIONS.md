# Reversible decisions

- Empty workspace; no existing code or ancestor AGENTS.md was found. Both supplied briefs were read completely. The handoff takes precedence where Balloon Rise moves into the first build.
- Initialized a local Git repository on `build/who-goes-first`. A verified source baseline is committed locally; no remote, push, or hosted workflow has been created.
- Astro 7, React 19, strict TypeScript 6, static JSON records, npm lockfile. Compatible stable versions were checked against npm metadata and official Astro docs. A local Node 22.23.2 package avoids the older installed Node's transitive engine incompatibilities.
- One picker island per tool page; no React runtime on rule pages. Search and house prompts use small progressive-enhancement scripts. Optional Balloon code imports only during use.
- Four numbered seats and Quick by default. Beyond 12 players the UI explicitly uses Instant, preserving every entry. Names are capped by validation at 24 grapheme clusters, never truncated. Duplicate suffixes derive from stable IDs.
- State is editing → ready → revealing → result. A synchronous activation guard protects even an instant draw from double activation. Presentation has no RNG. A bounded rejection loop fails safely if the secure source malfunctions.
- System reduced motion always wins; the app can reduce motion further. Sound is an original quiet oscillator chime, off initially. No licensed or third-party visual/sound assets.
- Preferences save locally; names save only with opt-in. Invalid partial edits stay in memory and clear the persisted roster. Forget resets the table; reset settings stops remembering without disrupting the visible group.
- Editorial review is required for house prompts and factual rules. On 2026-09-22 the owner authorized Codex to perform this review without individual human signatures. Source and revision checks still apply; see CONTENT_REVIEW.md. Public navigation does not advertise unavailable prompts.
- No contact address or real canonical domain was invented. Preview is noindex, with an empty sitemap. Production requires an explicit HTTPS canonical origin, maintained contact address, and host/logging disclosure; the actual values remain launch actions.
- On 2026-09-23 the owner chose to launch with the approved partial rule catalog while research continues. The public site must remain honest about its limited coverage. On 2026-09-24 the owner chose `https://who-goes-first.pages.dev` as the initial public domain and `edamame.makers@gmail.com` as the maintained contact, with a custom domain to follow later.
- No database, service worker, analytics sink, advertising, account, payment, remote AI, scheduled hosted job, external account change, or deployment.
- After the owner reported the missing compendium, expanded the three initial drafts to all 15 named candidates in the product plan. Twelve additional primary-source entries were researched on 2026-09-22, including edition-specific UNO exceptions and simultaneous-play games. No comprehensive external dataset was supplied. Sources and exact revisions require editorial review under the owner's later standing authorization; private evidence stays out of static output.
- Static rule records use Zod validation and explicit filesystem loading instead of an additional Astro collection abstraction. Only public field projections reach templates. Hash approval includes the substantive record and internal evidence, excluding lifecycle/approval metadata.
- The latest owner-directed refinement uses plum controls, warm paper and pastel game pieces, direct name editing and a roster that grows with the page. Help copy moved to Fairness; the homepage keeps only the tool and two directory links. Spinner, Card Draw and Towers now accompany Quick, Instant and Balloon Rise. Each presentation receives the same locked outcome and has no selection authority.
- Full compendium scope supersedes the old candidate-list cap. An inventory of 1,320 discovered game identities is tracked separately from researched rule records. Missing primary sources remain explicit; the task is not complete merely because identities were loaded. The random mix samples only its individually reviewed portable-criterion allowlist, without implying equal chances for people.
- Existing researched drafts require the same individual editorial review as new additions. The Rio Grande and Zoch review promotes fourteen exact revisions, six eligible for the random mix. Pechvogel's attributed address remains catalog-only because its usable location was not independently established; Niagara and the other simultaneous or role-based starts retain their phase context. This approval pass does not change research coverage or authorize deployment.
- Coverage identity overrides distinguish unrelated games with the same normalized title. Gamewright's Chomp! cannot satisfy the Allplay dinosaur game's inventory identity. Override references are validated and tested; these are private research metadata, not publication approvals.
- Editable name heights are written outside the ResizeObserver delivery cycle to avoid a loop reported by WebKit. Random-rule and house-rule buttons remain disabled until their handlers are installed, preventing ignored early clicks.
- All animated results keep their existing scene mounted beside the winner announcement. Spinner replaces the full editable roster with a compact name-and-color legend; Edit players restores the inputs without changing names. A clipped wheel viewport contains the rotated SVG bounds so twelve-player results fit at 320px without horizontal scrolling.
- CSP hashes cover executable inline scripts, not inert JSON-LD data blocks. This keeps the security header bounded as the catalog grows; the isolated release matrix now includes 41 clearly synthetic game pages.
- The expanded release fixture caught identical descriptions for games sharing a starting rule. Game and edition now qualify each description, preserving unique metadata without inventing different rules.
- Every build now audits generated HTML for metadata, exact canonicals, structured data, internal links, and sitemap parity. Lighthouse checks isolated production fixtures locally. Preference validation disables Zod's dynamic-code probe, preserving the strict CSP. Generated test artifacts are excluded from the development watcher to prevent preview restarts.
- Reproduced a separate development failure: fixture builds sharing node_modules invalidated Vite's optimized dependency cache, returning HTTP 504 and preventing picker hydration. Astro/Vite caches now live within each project and are separated by command. A fresh-browser regression check exercises the dev picker before and after release builds.
- Local navigation exposes the draft directory and house prompts directly, with visible review status. Production still uses only approved records. A failed picker download exposes a reload action; Firefox's automatic restoration of disabled controls is disabled on the picker form to avoid hydration mismatch.

- Newly added drafts exposed Astro's cached development `getStaticPaths`: directory links updated while detail routes returned 404. The local-only injected detail route now explicitly disables prerendering and reads the current draft per request. Regression coverage adds, edits and removes a synthetic draft without restarting. Static builds still contain no development routes.
- Newly approved records exposed the same cached-path behavior on `/games/[slug]/`. The `astro:route:setup` hook disables prerendering for that route only in local development; it reads the current approved catalog per request and returns a true 404 after removal. Builds still use `getStaticPaths` and static output. A separate synthetic approved-record regression checks additions, edits, removal and private-evidence exclusion. API reference: [Astro route setup](https://docs.astro.build/en/reference/integrations-reference/#astroroutesetup).
- Source availability checks now report total and checked counts, use three bounded workers, and provide explicit continuation for collections beyond 250 unique URLs. The previous silent 30-source cap was removed.
- Publisher source downloads are excluded from TypeScript and ESLint checks, matching their existing Git and deployment exclusion. Downloaded third-party scripts are evidence, not application modules; project research helpers and all application tests remain checked. The manual cache accepts an explicit bounded 1–150 MB retrieval limit for large official PDFs, with 35 MB as the default.

Official implementation references: [Astro React](https://docs.astro.build/en/guides/integrations-guide/react/), [routing](https://docs.astro.build/en/guides/routing/), [Playwright](https://playwright.dev/docs/intro). Publisher source URLs and page locations are recorded in each draft.

- The two standalone Perseverance episodes have distinct sourced answers but share the inventory’s combined-box identity. Explicit identity overrides preserve both answers without inflating completed-title counts. Trickerion’s top-hat setup chooser does not enter the random mix because opening play order is separately randomized.

- Cat-a-comb remains directory-only: its latest cat-comber places neutral pieces, then the opponent takes the first turn. Moon can enter the portable mix because its loudest-voice criterion selects the first sequential Construction turn, with the earlier Production Phase and two-player exception explicitly preserved.


- Core-game review retains the complete CATAN setup branches, UNO opening-card exceptions and Dominion rematch exception in the catalog. Tiny Towns appoints a first resource caller while placement is simultaneous; it stays outside the portable mix. Cascadia names all five source-confirmed animals so its mix criterion works without game components. Tests select remaining drafts dynamically rather than assuming a particular title can never be approved.

- Publication audits authorize game routes from validated approved records. An existing built page cannot authorize itself. Identical generic answer text may legitimately appear in approved and draft records; only exact approved text is exempted from the draft-text scan. Internal editorial evidence remains private for all records. Five regression tests and the isolated release matrix cover this correction.

- CGE setup markers and first choices are not automatically first-turn criteria. Alchemists, Bunny Bunny Moose Moose, Dungeon Lords, Dungeon Petz, Goblins, Inc. and Last Will retain their role, simultaneous-play or phase-order context in the catalog and stay outside the portable mix. Arnak, SETI, Tzolk'in and Age of Dirt qualify with their multiplayer, later-round, official tie and event-order distinctions preserved.


- Burgle Bros. identifies its English MarkIII-v2.05 file and second-edition credits; the separately retrieved German booklet does not establish the same criterion. The current Fowers store edition is not inferred onto the Burgle Bros 2 PDF. Isle of Trains identifies the original Dice Hate Me v8.6 booklet and stays out of the portable mix because the starter depends on a green engine card.
- R&R approval preserves Amalfi’s initial-resource, reverse-draft, later-period and solo distinctions. Touria’s two marriage criteria are alternatives, not a tie-break sequence. Pyramid Poker’s quoted instruction selects block placement under Set-up and remains catalog-only.
