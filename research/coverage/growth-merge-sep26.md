# Growth integration — 26 September 2026

Integrated `origin/claude/growth-automation` at `82f5c2a` into `codex/preview`, preserving the local homepage/theme checkpoint `d21eb25`, Amigo/Quick checkpoint `3f7a804`, and growth-report checkpoint `72b1fd0`. Then integrated current main at `c83c685`, including its separately reviewed popular-game batch, printables, visitor sharing, home-screen shortcut, and consented sharing measurement.

The combined inventory contains **5,006 identities, 957 approved edition records, 947 sourced identities, 4,059 pending identities, 370 portable criteria, and 60 house prompts**. These are integration counts, not new source approvals. Existing exact approval fingerprints are preserved. The overlapping Chess record keeps the newer reviewed Codex revision, including its explicit fixed-color/no-tie scope; both branch histories remain in the merge.

Both discovery sets are retained. Historical Wikidata reviews remain validated even when a later, independently sourced original discovery entry includes the same numeric identity. Existing deduplication includes each ID once. Explicit mappings keep original and Revised Agricola apart and prevent broad Netrunner and Mage Knight aliases from assigning those rules to unrelated games. No source text or editorial approval was generated to resolve a merge conflict.

Growth routes, per-rule share images, publisher links, similar-rule sections, optional Amazon boxes and funding links are retained. The picker, every method page and 404 explicitly disable ads. The existing discovery shelves, multi-edition sitemap filter, redirects, cache rules, PDF canonical header and manifest CSP permission are preserved. `themes` is reserved as a rule slug.

The alternate Google loader honors current and legacy analytics opt-outs and strips arbitrary query values before loading external tags. The default analytics path retains renewed consent for successful sharing. Revenue variables remain unchanged by this integration. Release fixtures exercise configured ads, affiliate and tip modes using synthetic values only.

Research priority follows `research/demand/search-console.json`: `missingRules` in order, verified identities from `unmatchedQueries`, and title/description work from `lowClickPages`. Demand never replaces a primary source or `CONTENT_REVIEW.md`.

Final integration gates passed: clean dependency installation, full verify (152 unit tests and 1,118 audited pages), the release matrix including configured revenue fixtures, and browser coverage across all714 cases. The full browser run passed692 cases; a focused rerun passed the remaining22 after stale test assertions were corrected. One WebKit stability timeout passed unchanged on the rerun. `VERIFICATION.md` records the exact split and preserved failure evidence. CI allows60minutes for the expanded serial suite, without changing individual test timeouts. Earlier checkpoint failures remain in their original logs and notes.
